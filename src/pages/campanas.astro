---
import Layout from '@layouts/Layout.astro';
import Header from '@components/Header.astro';
import Footer from '@components/Footer.astro';
import PageHead from '@components/PageHead.astro';
import Gallery from '@components/Gallery.astro';
import { PAGES_SEO } from '@/consts';
import { getTrabajosByCategory } from '@lib/trabajos';

const seo = PAGES_SEO.campanas;
const works = await getTrabajosByCategory('Campañas');
---

<Layout title={seo.title} description={seo.description}>
  <Header current="/campanas" />
  <main>
    <PageHead title="Campañas" count={works.length} />
    <Gallery filter="Campañas" />
  </main>
  <Footer />
</Layout>
